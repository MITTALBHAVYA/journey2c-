#include <bits/stdc++.h>
#include <string>
using namespace std;

int main()
{
    int t;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        long int n;
        scanf("%ld", &n)
            string s;
        getline(cin, s);
        sort(s.begin(), s.end(), greater<char>());
        char temp;
        temp = s[len]

               cout
               << s;
    }
    return 0;
}