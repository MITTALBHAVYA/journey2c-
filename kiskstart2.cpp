#include <bits/stdc++.h>
using namespace std;
int solve(vector<long long int>&x,int m,int r,int n)
{
    //base case
    if(max(0,x[0]-r)!=0)
    {
        return -1;
    }
    
}
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        long long int m,r,n;
        cin>>m>>r>>n;
        vector<long long int>x;
        unordered<int,int>mp;
        for(long long i=0;i<n;i++)
        {
            cin>>x[i];
        }

    }
}